import type { Metadata } from 'next';
import './globals.css';
import { Toaster } from '@/components/ui/toaster';

export const metadata: Metadata = {
  title: 'Verdant Agenda - Your AI-Powered Schedule Planner',
  description: 'Organize your day with Verdant Agenda, a smart and minimalist to-do list and schedule planner. Leverage AI to find the perfect time for your tasks and boost your productivity.',
  keywords: ['schedule planner', 'todo list', 'daily planner', 'task manager', 'AI scheduler', 'productivity app', 'Verdant Agenda'],
  icons: {
    icon: '/favicon.ico',
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="h-full">
      <head>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link
          rel="preconnect"
          href="https://fonts.gstatic.com"
          crossOrigin="anonymous"
        />
        <link
          href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"
          rel="stylesheet"
        />
      </head>
      <body className="font-body antialiased">
        {children}
        <Toaster />
      </body>
    </html>
  );
}
